[package]
name = "logger"
publish = false
version = "0.2.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
plugin-sdk = { version = "0.2.0", path = "../../crates/plugin-sdk" }
serde_json = "1.0.149"
